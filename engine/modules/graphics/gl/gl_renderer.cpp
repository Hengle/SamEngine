#include "gl_renderer.h"

namespace sam
{
    gl_renderer::gl_renderer()
    {
    }

    gl_renderer::~gl_renderer()
    {
    }

    void gl_renderer::apply_target(std::shared_ptr<texture_base> texture, const clear_state &state)
    {
    }
}